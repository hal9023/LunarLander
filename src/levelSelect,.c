#include "raylib.h"
#include "resource_dir.h"

int levelSelect (int level) {
    /* 
        Will want to draw a specific scene with buttons for this
        Will probably have to implement some kind of scene switcher logic
        Right now, the main loop in main.c JUST renders the game from start
        So maybe I will want a main menu?
    */
}